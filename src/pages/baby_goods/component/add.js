import React, {Component} from 'react'
import WrappedForm from './formModal'

class Add extends Component {
  constructor(props) {
    super(props)
  }

  render() {
    return ({})
  }
}

export default Add